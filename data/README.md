## data/

This directory stores raw and processed datasets used by the system.

Examples include:
- Raw JSON responses from the Nutritionix API
- Normalized food item records
- Intermediate files generated during preprocessing

Large or sensitive files may be excluded via `.gitignore`.
